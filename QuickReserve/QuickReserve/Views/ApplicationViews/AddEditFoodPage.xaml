<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="QuickReserve.Views.ApplicationViews.AddEditFoodPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}"
    BackgroundColor="#1C1C1C"
    NavigationPage.HasNavigationBar="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="PrimaryColor">#BB86FC</Color>
            <Color x:Key="PrimaryLightColor">#D1B4F9</Color>
            <Color x:Key="ErrorColor">#CF6679</Color>
            <Color x:Key="SurfaceColor">#2C2C2C</Color>
            <Color x:Key="SurfaceLightColor">#383838</Color>
            <Color x:Key="OnSurfaceColor">#FFFFFF</Color>
            <Color x:Key="SecondaryTextColor">#B0B0B0</Color>
            <Color x:Key="DisabledColor">#808080</Color>
            <Color x:Key="DisabledTextColor">#B0B0B0</Color>

            <!--  Label Style  -->
            <Style x:Key="FieldLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource PrimaryColor}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Margin" Value="0,10,0,5" />
            </Style>

            <!--  Entry Style  -->
            <Style x:Key="EntryStyle" TargetType="Entry">
                <Setter Property="BackgroundColor" Value="{DynamicResource SurfaceColor}" />
                <Setter Property="TextColor" Value="{DynamicResource OnSurfaceColor}" />
                <Setter Property="PlaceholderColor" Value="{DynamicResource SecondaryTextColor}" />
                <Setter Property="HeightRequest" Value="45" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <!--  Editor Style  -->
            <Style x:Key="EditorStyle" TargetType="Editor">
                <Setter Property="BackgroundColor" Value="{DynamicResource SurfaceColor}" />
                <Setter Property="TextColor" Value="{DynamicResource OnSurfaceColor}" />
                <Setter Property="PlaceholderColor" Value="{DynamicResource SecondaryTextColor}" />
                <Setter Property="HeightRequest" Value="100" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <!--  Picker Style  -->
            <Style x:Key="PickerStyle" TargetType="Picker">
                <Setter Property="BackgroundColor" Value="{DynamicResource SurfaceColor}" />
                <Setter Property="TextColor" Value="{DynamicResource OnSurfaceColor}" />
                <Setter Property="TitleColor" Value="{DynamicResource SecondaryTextColor}" />
                <Setter Property="HeightRequest" Value="45" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <!--  Button Style  -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{DynamicResource PrimaryColor}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HeightRequest" Value="50" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Margin" Value="0,10,0,10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource PrimaryColor}" />
                                    <Setter Property="TextColor" Value="White" />
                                    <Setter Property="Scale" Value="1" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="0.95" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource DisabledColor}" />
                                    <Setter Property="TextColor" Value="{DynamicResource DisabledTextColor}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!--  Delete Button Style  -->
            <Style x:Key="DeleteButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{DynamicResource ErrorColor}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HeightRequest" Value="50" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Margin" Value="0,10,0,10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource ErrorColor}" />
                                    <Setter Property="TextColor" Value="White" />
                                    <Setter Property="Scale" Value="1" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="Scale" Value="0.95" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource DisabledColor}" />
                                    <Setter Property="TextColor" Value="{DynamicResource DisabledTextColor}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout>
            <Grid
                Padding="20,15,20,5"
                ColumnDefinitions="Auto,*,Auto"
                RowDefinitions="Auto">
                <!--  Back Button  -->
                <ImageButton
                    Grid.Column="0"
                    BackgroundColor="Transparent"
                    Clicked="OnCancelClicked"
                    HeightRequest="40"
                    Source="back_icon.png"
                    VerticalOptions="Center"
                    WidthRequest="40" />

                <!--  Title  -->
                <Label
                    Grid.ColumnSpan="3"
                    FontAttributes="Bold"
                    FontSize="24"
                    HorizontalOptions="Center"
                    Text="{Binding Title}"
                    TextColor="{DynamicResource PrimaryColor}"
                    VerticalOptions="Center" />
            </Grid>
            <ScrollView>
                <StackLayout Padding="20" Spacing="10">
                    <!--  Food Name  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Név" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="NameEntry"
                                Placeholder="Adja meg az étel nevét"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.Name, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Description  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Leírás" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Editor
                                x:Name="DescriptionEditor"
                                Placeholder="Adja meg az étel leírását"
                                Style="{StaticResource EditorStyle}"
                                Text="{Binding Food.Description, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Ingredients  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Hozzávalók" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="IngredientsEntry"
                                Placeholder="Hozzávalók (vesszővel elválasztva, pl. vaj, só)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding IngredientsText, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Allergens  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Allergének" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="AllergensEntry"
                                Placeholder="Allergének (vesszővel elválasztva, pl. glutén, tej)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding AllergensText, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Calories  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Kalória" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="CaloriesEntry"
                                Keyboard="Numeric"
                                Placeholder="Adja meg a kalóriát (pl. 500)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.Calories, StringFormat='{0:F0}', Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Weight  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Súly" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="WeightEntry"
                                Placeholder="Adja meg a súlyt (pl. 200g)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.Weight, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Preparation Time  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Elkészítési idő" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="PreparationTimeEntry"
                                Keyboard="Numeric"
                                Placeholder="Elkészítési idő (perc, pl. 15)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.PreparationTime, Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Category  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Kategória" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Grid ColumnDefinitions="*,Auto">
                                <Picker
                                    x:Name="CategoryPicker"
                                    Title="Válasszon kategóriát"
                                    Grid.Column="0"
                                    Margin="0,0,5,0"
                                    SelectedItem="{Binding Food.Category, Mode=OneWay}"
                                    Style="{StaticResource PickerStyle}" />
                                <Button
                                    x:Name="AddCategoryButton"
                                    Grid.Column="1"
                                    Padding="0"
                                    BackgroundColor="#ffd700"
                                    Clicked="OnAddCategoryClicked"
                                    CornerRadius="8"
                                    FontAttributes="Bold"
                                    FontSize="16"
                                    HeightRequest="40"
                                    HorizontalOptions="End"
                                    Text="+"
                                    TextColor="#1C2526"
                                    VerticalOptions="Center"
                                    WidthRequest="40" />
                            </Grid>
                        </Frame>
                    </StackLayout>

                    <!--  Food Price  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Ár (€)" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="PriceEntry"
                                Keyboard="Numeric"
                                Placeholder="Adja meg az árat (pl. 12.50)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.Price, StringFormat='{0}', Mode=OneWay}" />
                        </Frame>
                    </StackLayout>
                    
                    <!--  Food Discounted Price  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Discounted Price (€)" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <Entry
                                x:Name="DiscountedPriceEntry"
                                Keyboard="Plain"
                                Placeholder="Adja meg az árat (pl. 12.50)"
                                Style="{StaticResource EntryStyle}"
                                Text="{Binding Food.DiscountedPrice, StringFormat='{0}', Mode=OneWay}" />
                        </Frame>
                    </StackLayout>

                    <!--  Food Availability  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Elérhető" />
                        <Frame
                            Padding="10"
                            BackgroundColor="{DynamicResource SurfaceColor}"
                            CornerRadius="10">
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Label
                                    FontSize="16"
                                    Text="Elérhető"
                                    TextColor="{DynamicResource OnSurfaceColor}"
                                    VerticalOptions="Center" />
                                <Switch
                                    x:Name="swIsAvailable"
                                    HorizontalOptions="EndAndExpand"
                                    IsToggled="{Binding Food.IsAvailable, Mode=OneWay}"
                                    OnColor="#4CAF50" />
                            </StackLayout>
                        </Frame>
                    </StackLayout>

                    <!--  Food Image  -->
                    <StackLayout Spacing="5">
                        <Label Style="{StaticResource FieldLabelStyle}" Text="Kép" />
                        <Grid
                            HeightRequest="120"
                            HorizontalOptions="Center"
                            WidthRequest="120">
                            <Image
                                x:Name="FoodImage"
                                Aspect="AspectFill"
                                HeightRequest="120"
                                Source="{Binding Food.ImageSource}" />
                            <Image
                                HeightRequest="40"
                                HorizontalOptions="End"
                                Source="edit_icon.png"
                                TranslationX="20"
                                VerticalOptions="End"
                                WidthRequest="40">
                                <Image.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnEditFoodImageClicked" />
                                </Image.GestureRecognizers>
                            </Image>
                        </Grid>
                    </StackLayout>

                    <!--  Save Button  -->
                    <Button
                        x:Name="SaveButton"
                        Clicked="OnSaveClicked"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Text="Mentés" />

                    <!--  Delete Button  -->
                    <Button
                        x:Name="DeleteButton"
                        Clicked="OnDeleteClicked"
                        IsVisible="{Binding IsEditMode}"
                        Style="{StaticResource DeleteButtonStyle}"
                        Text="Törlés" />
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>